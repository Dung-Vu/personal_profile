# Check-In

Short-term live coordination for `personal-website`. Keep this file compact.

## Active Claims

| Owner | Task | Started | Status |
| :---- | :--- | :------ | :----- |
| none | - | - | idle |

## Current Status

- Repo path: `d:\personal-website`
- Branch: `develop`
- Stack: Vite 7 + React 19 + GSAP/ScrollTrigger + lucide-react, pnpm
- Routes: `/`, `/about`, `/work`, `/work/:slug`, `/stack`, `/workflow`, `/contact`, `/lab` (archive), `/404`
- Deploy target: Cloudflare Pages at `dinhdung.dev`

## Next Steps

- Keep the main portfolio bright, refined and trust-focused. Lab stays archived.
- Re-run `npm run build && npm run prerender && npm run qa:perf && npm run qa:a11y` after layout or content changes.
- Re-run `npm run assets:audit` after image changes.

## New Session Brief

- Start from real source, not memory only.
- Read this file first, then inspect `src/pages/CaseDetailPage.jsx`, `src/styles/case-detail.css`, `src/content/projects.js`.
- Run `npm run build` for any code/style/content change.
- Do not focus on deployment unless explicitly requested.

## Archive

Historical completed-work and verification logs moved to `archive/checkin-snapshots/2026-05-10.md`.