name = "personal-profile"
pages_build_output_dir = "dist"
compatibility_date = "2026-05-09"

[env.production]
# Build config managed via Cloudflare Dashboard (Git-connected Pages)
# Build command: npm run build
# Output directory: dist
