---
description: Update daily work log with current progress, decisions, and blockers
agent: build
---

Review the current session's work:
1. Read `docs/CHECK_IN.md` and any recently modified files (check `git diff --stat`).
2. Read the latest daily log in `memory/` (or create `memory/YYYY-MM-DD.md` from `memory/DAILY_LOG_TEMPLATE.md` if today's doesn't exist).
3. Update today's daily log with:
   - Tasks worked on (time, status, notes)
   - Completed items
   - In-progress items with current state
   - Blockers or issues encountered
   - Key decisions and architectural insights
   - Next session priority
4. Keep it compact - each entry 1-2 lines max.
5. Update `docs/CHECK_IN.md` if active claims or status changed.
